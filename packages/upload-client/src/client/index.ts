/**
 * Upload Client
 * 
 * Re-exports the upload client class for convenient importing.
 * This allows consumers to import the client directly from the main package.
 * 
 * @example
 * ```typescript
 * import { UploadClient } from '@tutenet/upload-client';
 * ```
 */

export * from './uploadClient';